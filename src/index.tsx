import * as React from "react";
import * as ReactDOM from "react-dom";

import App from "./view/app";

ReactDOM.render(
    <App title="Simple Webpack for React, TypeScript & SASS"/>, document.getElementById("root")
);